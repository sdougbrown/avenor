{
  "version": 1,
  "fields": {
    "schema_version": {
      "required": true,
      "isEmpty": "number"
    },
    "template_id": {
      "required": true,
      "isEmpty": "string"
    },
    "template_version": {
      "required": true,
      "isEmpty": "string"
    },
    "metadata": {
      "isEmpty": "object"
    },
    "entry_nodes": {
      "required": true,
      "isEmpty": "array"
    },
    "nodes": {
      "required": true,
      "isEmpty": "array"
    },
    "terminal_outcomes": {
      "required": true,
      "isEmpty": "array"
    },
    "bounded_loops": {
      "isEmpty": "array"
    },
    "default_lease_policy": {
      "isEmpty": "object"
    },
    "default_retry_policy": {
      "isEmpty": "object"
    },
    "composition_limits": {
      "isEmpty": "object"
    }
  },
  "rules": [
    {
      "type": "fairWhen",
      "field": "schema_version",
      "when": {
        "op": "eq",
        "field": "schema_version",
        "value": 1
      },
      "reason": "workflow schema_version must be 1"
    }
  ]
}
