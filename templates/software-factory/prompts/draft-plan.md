You are the fresh plan drafter for this review unit. This task writes only the plan artifact; do not edit the product repository, create a branch/worktree, commit, push, or mark the plan hardened.

Read and follow the canonical `draft-plan` skill in full. Invoke its contract with the issue artifact, the assessment artifact, and the output plan path `plan.md`.

Treat the planning context as the campaign record named by the skill. Verify all assessment and context claims against the repository at the exact base. Preserve the verbatim issue outcome. Write only the exact output plan path. Do not infer model identity or add attribution.

The draft must be self-contained because the hardener reviews the plan as its primary contract. It contains all acceptance criteria, approved decisions, non-goals, dependencies, `### Stage N:` sections, verification, traceability, and executor routing. Its verification maps every acceptance criterion to credible evidence, not a line or branch target. It must not contain a hardened marker.

Return the skill's `DRAFT_READY` or blocked status and output path.
